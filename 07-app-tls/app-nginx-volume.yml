apiVersion: v1
kind: PersistentVolume
metadata:
  name: app-nginx-pv
spec:
  capacity:
    storage: 256Mi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  persistentVolumeReclaimPolicy: Retain
  local:
    path: ${APP_NGINX_DATA}
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - ${HOSTNAME}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-nginx-storage-claim
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  volumeName: app-nginx-pv
  resources:
    requests:
      storage: 256Mi
